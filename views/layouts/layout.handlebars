<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Rubik:wght@400;700;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/app.css">
    <link rel="stylesheet" type="text/css" href="https://unpkg.com/trix@2.0.8/dist/trix.css">
    <script type="text/javascript" src="https://unpkg.com/trix@2.0.8/dist/trix.umd.min.js"></script>
    <title>{{namePage}}</title>
</head>
<body>
    <div class="name-site container">
        <h1>
            <a href="/">LookPosting</a>
        </h1>

        {{#if bar}}
            <div class="finder">
                <form action="/search" method="POST">
                    <input type="text" name="q" class="search" placeholder="Search by vacancy names">
                    <input type="submit" value="search">
                </form>
            </div>

        {{/if}}
        {{#if log_out}}
            <div class="log-out">
                <p>Hi, {{name}}</p>
                {{#if image}}
                    <img src="/uploads/profiles/{{image}}" class="admin-profile">
                {{/if}}
                <a href="/management">Management</a>
                <a href="/log-out">Log out</a>
            </div>
        {{/if}}
    </div>

    <header class="site-header container separator">
        <h2>{{namePage}}</h2>
        {{#if tagline}}
            <p class="tagline">{{tagline}}</p>
        {{/if}}
        {{#if button}}
            <a href="/vacancies/new" class="btn btn-blue"> Publish new vacancy</a>
        {{/if}}
    </header>

    <div class="main-content container">
        {{#if messages}}
            <div class="alerts">
                {{#showAlerts messages}}
                {{/showAlerts}}
            </div>
        {{/if}}
        {{{body}}}
    </div>
    <script src="/dist/bundle.js"></script>
</body>
</html>